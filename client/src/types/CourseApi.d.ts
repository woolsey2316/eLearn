type CourseAPI = {
  examResults: Array<>;
  subscriber: Array<string>;
  exams: Array<>;
  _id: string;
  CourseName: string;
  instructor: string;
  category: string;
  expires: Date;
  description: string;
  status: boolean;
};
export const CourseAPI;
